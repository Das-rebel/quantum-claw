{
  "name": "omniclaw-cookie-refresh",
  "version": "1.0.0",
  "description": "Cookie refresh service for OmniClaw scrapers",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "gcloud functions deploy cookieRefresh --runtime nodejs18 --trigger-http --allow-unauthenticated --region asia-south1 --set-env-vars COOKIE_REFRESH_API_KEY=omniclaw-cookie-refresh-2024"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
