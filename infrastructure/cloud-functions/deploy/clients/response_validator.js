/**
 * Response Validator Stub
 * Placeholder for response validator module
 */
class ResponseValidator {
  constructor(options = {}) {
    // No-op in stub
  }

  validate(response) {
    return { valid: true, response };
  }
}

module.exports = ResponseValidator;
